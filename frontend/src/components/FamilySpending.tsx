import type { DashboardData } from "../types/dashboard";
function FamilySpending({ members, expenses }: { members: DashboardData["memberSpending"]; expenses: number }) {
  return <div className="rounded-2xl border border-[#e8e5ef] bg-white p-5 shadow-sm transition-shadow duration-300 hover:shadow-md sm:p-6"><div><p className="text-sm font-semibold text-[#2a234f]">Family Spending</p><p className="mt-1 text-xs text-[#77738a] sm:text-sm">Expenses by family member</p></div>{members.length === 0 ? <div className="mt-5 rounded-xl bg-[#f8f7fb] px-5 py-8 text-center text-sm text-[#77738a]">Family spending will appear here once expenses are added.</div> : <div className="mt-5 space-y-4">{members.map((member) => <div key={member.member}><div className="mb-2 flex items-center justify-between gap-3 text-sm"><span className="truncate font-medium text-[#2a234f]">{member.member}</span><span className="shrink-0 font-semibold text-[#2a234f]">₹{member.amount.toLocaleString("en-IN")}</span></div><div className="h-2 overflow-hidden rounded-full bg-[#f8f7fb]"><div className="h-full rounded-full bg-[#ffb3c3] transition-all duration-700" style={{ width: `${expenses ? Math.min((member.amount / expenses) * 100, 100) : 0}%` }} /></div></div>)}</div>}</div>;
}
export default FamilySpending;
